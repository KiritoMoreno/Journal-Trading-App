<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_logo"
        android:label="@drawable/ic_logo"
        android:roundIcon="@mipmap/ic_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TradingJournalPruebaFinal"
        tools:targetApi="31">
        <activity
            android:name=".Journal.DetailedActivity"
            android:exported="false" />
        <activity
            android:name=".Journal.AddTransactionActivity"
            android:exported="false" />
        <activity
            android:name=".Graphics.GraphicsActivity"
            android:exported="false" />
        <activity
            android:name=".Calculator.CalculatorActivity"
            android:exported="false" />
        <activity
            android:name=".Analysis.AnalysisActivity"
            android:exported="false" />
        <activity
            android:name=".Journal.JournalActivity"
            android:exported="false" />
        <activity
            android:name=".HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>